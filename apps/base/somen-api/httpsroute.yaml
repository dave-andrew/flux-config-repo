apiVersion: gateway.networking.k8s.io/v1
kind: HTTPRoute
metadata:
  name: somen-backend-httpsroute
  labels:
    gateway: traefik
spec:
  parentRefs:
  - name: traefik
    sectionName: websecure
    namespace: traefik
  hostnames:
  - somen-api.kube.slc.net
  rules:
  - matches:
    - path:
        type: PathPrefix
        value: /
    backendRefs:
    - name: somen-backend-service
      port: 8080
