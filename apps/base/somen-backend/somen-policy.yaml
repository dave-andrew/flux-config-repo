---
apiVersion: image.toolkit.fluxcd.io/v1beta2
kind: ImagePolicy
metadata:
  name: somen-backend-policy
spec:
  imageRepositoryRef:
    name: somen-backend-repo
  policy:
    semver:
      range: ">=0.0.0"