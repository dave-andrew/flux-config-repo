apiVersion: image.toolkit.fluxcd.io/v1beta2
kind: ImagePolicy
metadata:
  name: call-na-frontend-policy
spec:
  imageRepositoryRef:
    name: call-na-frontend-repo
  policy:
    semver:
      range: ">=0.0.0"