0apiVersion: helm.toolkit.fluxcd.io/v2
kind: HelmRelease
metadata:
  name: kubernetes-dashboard-release
  namespace: kubernetes-dashboard
spec:
  interval: 1h
  chart:
    spec:
      version: "*"
      sourceRef:
        kind: HelmRepository
        name: kubernetes-dashboard-repository
  values:
    kong:
      enabled: true
      proxy:
        type: LoadBalancer
        http:
          enabled: true
